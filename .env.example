# Copy this file to .env.local and replace the dummy webhook URL there.
# Do not put the real Slack webhook URL in README.md or launchd plist files.
SLACK_WEBHOOK_URL="https://hooks.slack.com/services/EXAMPLE/EXAMPLE/EXAMPLE"
MAX_ITEMS_PER_RUN=1
DRY_RUN=1
